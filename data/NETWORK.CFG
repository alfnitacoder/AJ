# AJOS Network Configuration
# Lines starting with # are comments
#
# Static IP configuration (comment out to use DHCP)
ip 10.0.2.15

# DNS server (optional, will be auto-configured via DHCP if not set)
dns 8.8.8.8

# Gateway (for future routing support)
# gateway 10.0.2.2
