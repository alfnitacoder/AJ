.DS_Store
*.log
build/
*.bak
*.backup
openssh-portable/
reference/
# Optional LLM weights (download via Makefile; too large for GitHub)
data/STORIES*.BIN
data/TOK*.BIN
# User demo programs (built into build/, copied to data/ at image build)
data/FORKDEMO.BIN
data/CHILD.BIN
